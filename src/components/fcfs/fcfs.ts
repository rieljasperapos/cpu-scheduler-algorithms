import { Process } from "@/types/process-types";
import { ProcessResult } from "@/types/process-types";

export function FCFS(processes: Process[]) {
  // TODO: Implement FCFS algorithm
  return processes;
}